language: c++

dist: xenial

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      
script:
  - cd ../../..
  - ls
  - cd build
  - ls
  - cd virtualenv
  - ls
  - make -f testing_make
  - ./bin/maintest
